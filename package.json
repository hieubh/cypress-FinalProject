{
  "name": "cypress-hieubh-finalproject",
  "version": "1.0.0",
  "main": "cypress.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^17.1.1",
    "@cypress/browserify-preprocessor": "^3.0.2",
    "typescript": "^5.1.3"
    },
    "cypress-cucumber-preprocessor": {
    "json": {
    "enabled": false,
    "output": "cypress/cucumberReports/result.json"
    },
    "stepDefinitions": [
    "cypress/e2e/tests/step_definitions/*.{js,ts}"
    ]
    }
}
